{
  "name": "git-amida",
  "displayName": "GitAmida",
  "description": "Review Git history and open commit diffs without hiding the history panel.",
  "version": "0.0.1",
  "private": true,
  "publisher": "hirosco",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/hirosco/git-amida.git"
  },
  "engines": {
    "vscode": "^1.100.0"
  },
  "extensionDependencies": [
    "vscode.git"
  ],
  "categories": [
    "SCM Providers",
    "Visualization"
  ],
  "main": "./dist/src/extension.js",
  "files": [
    "dist/src/**/*.js",
    "dist/webview/**/*.js",
    "media/**",
    "README.md"
  ],
  "contributes": {
    "commands": [
      {
        "command": "gitAmida.open",
        "title": "GitAmida: Open",
        "icon": "$(git-commit)"
      },
      {
        "command": "gitAmida.refresh",
        "title": "GitAmida: Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "gitAmida.switchBranch",
        "title": "Switch Branch…",
        "category": "GitAmida",
        "icon": "$(git-branch)"
      }
    ],
    "viewsContainers": {
      "panel": [
        {
          "id": "gitAmida",
          "title": "GitAmida",
          "icon": "media/git-amida.svg"
        }
      ]
    },
    "views": {
      "gitAmida": [
        {
          "id": "gitAmida.history",
          "name": "History",
          "type": "webview",
          "icon": "media/git-amida.svg",
          "contextualTitle": "GitAmida"
        }
      ]
    },
    "menus": {
      "webview/context": [
        {
          "command": "gitAmida.switchBranch",
          "when": "webviewId == 'gitAmida.history' && webviewSection == 'commit' && gitAmidaHasSwitchableBranch",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "npm run build:extension && npm run build:webview",
    "build:extension": "tsc -p tsconfig.json",
    "build:webview": "tsc -p tsconfig.webview.json",
    "check": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.webview.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "package:inspect": "vsce ls --no-dependencies",
    "package:vsix": "vsce package --no-dependencies --skip-license"
  },
  "devDependencies": {
    "@types/node": "24.8.1",
    "@types/vscode": "1.100.0",
    "@vscode/vsce": "3.9.2",
    "typescript": "5.9.3"
  },
  "allowScripts": {
    "@vscode/vsce-sign@2.0.9": false,
    "keytar@7.9.0": false
  },
  "packageManager": "npm@11.16.0"
}
