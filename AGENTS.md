# AGENTS.md

## プロジェクト概要

GitAmidaは、複数のGitコミットを一つの変更単位として確認するための、Go製Terminal UIです。汎用Gitクライアントではなく、履歴・変更ファイル・diffの閲覧に集中します。

現在は設計・MVP準備段階です。実装前に`README.md`、`DESIGN.md`、`ROADMAP.md`を確認してください。

## 正本となる文書

- `AGENTS.md`: 開発手順、規約、落とし穴
- `DESIGN.md`: 現在のアーキテクチャと非自明な判断理由
- `ROADMAP.md`: この先に実装する内容と理由

完了履歴や判断ログを別ファイルへ追記しません。現在の真実は上記文書を上書きし、過去の経緯はGit履歴へ委ねます。

## 実装原則

- 最小変更を優先し、無関係なリファクタリングを行わない
- GitAmida本体はGoで完結させる
- UI、Git実行、ドメインロジック、外部ツール連携を分離する
- Bubble Teaの`Update`をブロックせず、I/Oはキャンセル可能なコマンドとして実行する
- マウス操作には必ず同等のキーボード操作を用意する
- 端末幅や機能が不足する場合は、安全に機能を縮退させる
- Git履歴を変更する操作を、明示的な設計変更なしに追加しない

## 想定ディレクトリ

```text
cmd/git-amida/       CLIエントリーポイント
internal/app/        ユースケースと非同期処理の調停
internal/domain/     コミット選択、変更集約、表示モデル
internal/git/        Git CLIの実行と出力解析
internal/tui/        Bubble Teaの画面、入力、描画
internal/diff/       diffモデルと表示形式
internal/opener/     内部・外部diffオープナー
internal/config/     設定の読み込みと検証
testdata/            解析用の固定fixture
```

初期実装で不要なパッケージは先に作りません。

## Git CLIの扱い

- シェル文字列を組み立てず、`exec.CommandContext`へコマンドと引数を分けて渡す
- パス引数の前に`--`を置き、revisionとpathの曖昧性を避ける
- 解析対象の出力では色、pager、外部diffを無効化する
- Gitの終了コードと標準エラーを保持し、ユーザー向けエラーへ変換する
- 大きな出力には上限、遅延取得、キャンセルを用意する
- ユーザーのglobal Git設定に依存した解析結果を避ける
- 初期スコープではread-onlyコマンドのみ許可する

## diffの規約

- 単一コミットは原則として親から選択コミットまでを比較する
- root commitはempty treeと比較する
- 連続範囲は最古コミットの直前から最新コミットまでの最終差分を表示する
- 非連続選択から仮想ツリーを作らず、変更ファイルを集約してコミット別diffを示す
- Unified、Side-by-side、Word diffが同じdiffモデルを利用する
- 空白無視の設定は表示だけでなくdiff生成条件として明示する
- rename、delete、binary、submodule、大きすぎるdiffを通常のテキスト変更と区別する

## 外部ツール連携

- 外部ツールは`opener`インターフェースの実装として追加する
- `code`、Kaleidoscope、任意コマンドをGitロジックへ直接埋め込まない
- 外部プロセスはシェルを介さず実行する
- Git blobから作る一時ファイルは作業専用ディレクトリへ置き、権限と削除時期を管理する
- ツールが見つからない場合は内部diffへフォールバックし、勝手にインストールしない

## テスト

- ドメインロジックはGitやTUIから独立したtable-driven testで確認する
- Git解析は固定fixtureと、一時ディレクトリに作る小さなGitリポジトリで確認する
- ユーザーの実リポジトリやglobal Git設定をテストで変更しない
- TUIは状態遷移と主要画面のgolden testを優先する
- マウスだけでなくキーボード経路もテストする

## UIと権利への配慮

- 既存IDEの便利な操作思想は、GitAmida自身の要件へ抽象化して実装する
- 特定製品の画面配置、文言、アイコン、配色、画像を忠実に複製しない
- JetBrains製品のコード、素材、スクリーンショットを無断で取り込まない
- 特定製品との提携や互換製品であると誤認させる名称・表示を使わない

## Git運用

- commit messageは英語のConventional Commitsを使う
- 新規ブランチは原則`feature/<topic>`とする
- ブランチ切り替えは`git switch`、ファイル復元は`git restore`を使う
- ユーザーの既存変更を保持し、タスク外の変更を混ぜない
